CREATE USER '<%= node['mysql-custom']['database']['admin_username'] %>'@'%' IDENTIFIED BY '<%= @passwords['admin_password'] %>';
GRANT ALL PRIVILEGES ON *.* TO <%= node['mysql-custom']['database']['admin_username'] %>@'%' WITH GRANT OPTION;
use <%= node['mysql-custom']['database']['dbname'] %>;
FLUSH PRIVILEGES;
create table testdata (
  id int not null auto_increment primary key,
  foo varchar(25),
  bar int);
insert into testdata values(null, 'hello', 12345);